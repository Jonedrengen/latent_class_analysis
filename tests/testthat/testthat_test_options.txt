# testthat expectation options

expect_equal(actual, expected, tolerance = NULL, info = NULL, label = NULL, expected.label = NULL)
expect_identical(actual, expected, info = NULL, label = NULL, expected.label = NULL)
expect_true(object, info = NULL, label = NULL)
expect_false(object, info = NULL, label = NULL)
expect_null(object, info = NULL, label = NULL)
expect_type(object, type)
expect_s3_class(object, class)
expect_s4_class(object, class)
expect_length(object, n)
expect_named(object, expected.names = NULL, ignore.order = FALSE, ignore.case = FALSE)
expect_match(object, regexp, all = TRUE, ...)
expect_warning(object, regexp = NULL, ...)
expect_error(object, regexp = NULL, ..., class = NULL)
expect_message(object, regexp = NULL, ...)
expect_output(object, regexp = NULL, ...)
expect_silent(object)
expect_visible(object)
expect_invisible(object)
expect_is(object, class)
expect_lt(object, expected)
expect_lte(object, expected)
expect_gt(object, expected)
expect_gte(object, expected)
expect_setequal(object, expected)
expect_contains(object, expected)
expect_mapequal(object, expected)
expect_vector(object, ptype = NULL, size = NULL)
expect_snapshot(x, cran = FALSE)
expect_snapshot_output(x, cran = FALSE)
expect_snapshot_error(x, class = NULL, cran = FALSE)
expect_snapshot_warning(x, class = NULL, cran = FALSE)
expect_snapshot_value(x, style = c("json", "json2", "deparse", "serialize"), cran = FALSE)
expect_snapshot_file(path, name = basename(path), compare = NULL, variant = NULL, cran = FALSE)

# testthat helper / control options
local_reproducible_output(width = 80, unicode = NULL, crayon = FALSE)
with_reporter(reporter, code)
test_that(desc, code)
skip(message)
skip_if_not(condition, message)
skip_if(condition, message)
skip_on_ci()
skip_on_cran()
skip_on_os(os)
skip_if_offline()
skip_if_not_installed(pkg, minimum_version = NULL)
setup(code)
teardown(code)
